[[plugin_info]]

defined('MOODLE_INTERNAL') || die();

$plugin->version   = [[version]];
$plugin->requires  = [[requires]]; // Requires this Moodle version
$plugin->component = '[[component]]';
$plugin->release   = [[release]];
$plugin->dependencies = array();
